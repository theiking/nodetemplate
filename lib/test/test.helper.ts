// import { connect, connection } from 'mongoose';

// connect('');

// connection.once('open', () => console.log('MongoDB'))
//   .on('error', error => console.log('Got an error', error));
  

// beforeEach(() => {
//   connection.collection.user.drop(()=> done());
// });  

