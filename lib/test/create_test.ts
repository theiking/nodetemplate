// import 'mocha';

// import * as assert from 'assert';
// import User from '../lib/modules/schema/userSchema';

// describe('test add new user', () => { 
//   let huy;

//   it('Demo some test', () => { 
//     assert(1 == 1);

//   })

//   it('Insert new user', () => { 
//     huy = new User({ name: 'Harvey Huy' });
//     huy.save().then(() => User.find({ name: 'Harvey Huy' }))
//       .then(users => assert(huy._id.toString() === users[0]._id.toString()))
//       .catch(()=> assert(false))
//       .then(()=> done());
//   })

// });